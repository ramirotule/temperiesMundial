services:
  # 1. Backend API (Node.js + Express + Prisma)
  - type: web
    name: prode-backend
    env: node
    plan: starter # Render free database requires starter or web services can link to databases
    rootDir: server
    buildCommand: npm install && npx prisma generate
    startCommand: npm start
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: prode-db
          property: connectionString
      - key: PORT
        value: 3001

  # 2. Frontend React (Vite + Tailwind CSS v4)
  - type: web
    name: prode-frontend
    runtime: static
    rootDir: client
    buildCommand: npm install && npm run build
    staticPublishPath: dist
    envVars:
      - key: VITE_API_URL
        value: https://temperies-mundial-backend.onrender.com

databases:
  # 3. PostgreSQL Database
  - name: prode-db
    plan: free
    postgresMajorVersion: 15
